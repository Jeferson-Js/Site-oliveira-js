/*const mysql = require('mysql');

const connection = mysql.createConnection({
host: 'localhost',
user: 'root',
database: 'formulario-js',
password: ''
});

connection.connect(function(error){
    if(error){
        throw error;
    }else{
        console.log("Conexao realizada com sucesso");
    }
});

connection.end();*/

//Como fazer a coneção do banco de dados com mysql em node.js